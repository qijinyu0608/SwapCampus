# 部署与运维手册

## 1. 环境要求
- Docker Desktop 24+
- Node.js 20+
- npm 10+

补充说明：
- 如果仅以 Docker 方式交付给别人，必需环境只有 `Docker Desktop`
- `Node.js` 和 `npm` 主要用于本机源码开发、宿主机构建和单独调试

## 1.1 工程化资产
- `docker-compose.yml`：基础设施即代码定义
- `Makefile`：统一工程命令入口
- `.github/workflows/ci.yml`：持续集成定义
- `backend/prisma/schema.prisma`：数据库结构定义

## 2. 本地部署步骤
```bash
docker compose up -d --build
```

说明：
当前项目已完成以下验证：
- 前端宿主机构建成功
- 后端测试通过
- 当前 Docker Compose 运行中的 frontend / backend / mysql / minio 服务可访问
- `db-init` 已调整为 `init` profile 下的一次性初始化任务
- 初始化与日常运行已分离，后端重启不会再重复触发数据库 reset
- 后端健康接口、商品接口、订单接口、消息接口、后台审核接口可访问

补充说明：
- 前端 `package.json` 已将宿主机构建固定到 `Node 20`
- 前端 Docker 交付当前采用 `build -> dist -> nginx` 的静态发布方式
- 当前前端生产镜像已完成 Docker 重建复验
- 本机 MySQL 通过 Docker Compose 提供，无需单独安装本地服务
- 如果数据库中已存在用户数据，`db-init` 会跳过演示数据初始化，避免覆盖已有新增数据
- SuperTokens 认证数据通过独立 PostgreSQL 容器持久化，`restart-auth` 不会清空已注册账号

如果 `make up` 失败，请先确认 Docker Desktop 已启动，并且本机存在可用的 Docker daemon。

建议改为：
```bash
docker compose --profile init up -d --build db-init
docker compose up -d --build mysql minio meilisearch supertokens-db supertokens backend frontend
```

如果需要源码依赖一起安装，再执行：
```bash
make install
```

## 3. 服务说明
- Frontend: `http://10.66.0.11:5178`
- Backend: `http://10.66.0.11:3001/api/health`
- Socket.IO: `http://10.66.0.11:3001`
- MinIO Console: `http://localhost:9001`
- MySQL: `localhost:3306`
- SuperTokens PostgreSQL: `localhost:5433`
- Db Init: 一次性初始化服务，不单独暴露端口

补充说明：
- 前端容器内部监听 `80`
- 宿主机 `5178` 通过 Compose 映射到前端容器 `80`
- 当前默认不预置任何演示账号、商品或校园服务数据

## 3.1 首次部署与二次部署区别
- 首次部署：自动建表并启动前后端与认证基础设施
- 二次部署：保留现有 MySQL 与 SuperTokens PostgreSQL 数据卷，不追加演示数据
- 如果要恢复到空库状态，需要删除卷后重新启动

重置命令：
```bash
docker compose down -v
docker compose up -d --build
```

## 4. 关闭服务
```bash
make down
```

等价命令：
```bash
docker compose down
```

## 5. 运维验证清单
- 访问 `http://localhost:3001/api/health`，返回 `status: ok`
- 访问 `http://localhost:3001/api/products/dashboard`，应返回商品数、用户数、待审核商品数
- 访问 `http://localhost:3001/api/products/publishing-rules`，应返回允许分类、禁售词和白名单规则
- 登录后调用 `GET /api/messages/conversations` 可查看当前账号的会话列表
- 登录后调用 `POST /api/messages/conversations/:id` 可发送一条站内消息
- 调用 `PATCH /api/admin/products/:id/status` 可执行审核通过或下架
- 调用 `POST /api/campus-services/:id/accept`、`POST /api/campus-services/:id/complete` 可验证校园服务任务流转
- 调用 `GET /api/users/:id/trust-summary`、`PATCH /api/users/:id/profile` 可验证个人中心资料与信用摘要接口
- 空库默认统计值应为：`userCount=0`、`productCount=0`、`pendingCount=0`
- 账号注册后再执行登录、发布和后台接口验证

建议补充一轮容器级检查：
- `docker compose ps` 中 `mysql`、`backend`、`frontend` 状态应为 `running`
- `docker compose ps` 中 `supertokens-db`、`supertokens` 状态也应为 `running`
- `db-init` 状态应为 `exited (0)`
- `docker compose logs backend --tail=50` 不应持续出现数据库连接报错
- 首页、登录页、想要页、发布页、后台页可正常打开
- `docker compose logs frontend --tail=50` 不应持续出现 `nginx` 启动错误

## 6. 后续维护项
- MinIO bucket 初始化
- 站内聊天 Socket.IO 补全已读回执、在线状态和断线补偿
- 更细粒度的举报处罚策略、申诉处理与审核台账增强
- 想要收藏后端持久化与跨端同步

## 6.1 文档与运维同步原则
- 每次部署方式、端口、账号、初始化逻辑或验证命令发生变化时，同步更新本手册。
- 每轮交付前先核对本手册、`16-交付清单.md` 与真实运行态是否一致。
- 运维文档不仅说明“怎么启动”，还要保留“如何验证启动结果”和“当前版本的已知边界”。
- 文档标准与版本维护机制见 `18-文档管理与版本规范.md`。

## 6.2 对外分发建议
- 分发内容：整个 `SwapCampus` 项目目录
- 推荐附带：`README.md`、`docs/08-部署与运维手册.md`、页面截图
- 对外说明重点：
  - 默认端口：`5178 / 3001 / 3306 / 9001`
  - 首次启动时间会比二次启动更长，因为需要建表与导入数据
  - 若端口冲突，需要先停止本机已有服务后再启动
  - 若前端镜像构建阶段因 Docker Hub 网络波动失败，可单独重试 `docker compose build frontend`

## 7. 容量评估参考
当前版本不虚构正式压测结果，但已补充轻量压测方案：
- 方案文件：`infra/load-test-plan.md`
- 建议容量表达：单机单实例适合小规模试用，目标并发为几十级
- 需要进一步扩容时，可按“缓存 -> 消息队列 -> 多实例后端 -> 数据库优化”的顺序演进
