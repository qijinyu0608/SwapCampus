# Agent Docs

请优先阅读：

- 仓库根目录 `AGENT.md`
- 当前目录下的 `INDEX.md`

`agent/` 目录用于沉淀当前有效的工程规范，而不是课程交付文档。

补充说明：

- 当前课程文档入口在 `docs/00-文档目录.md`
- 历史过程材料在 `docs/archive/`
- 当 `agent/`、`docs/` 和 `docs/archive/` 口径不一致时，优先级为：`agent/` > 当前源码/现行 `docs/` > `docs/archive/`
