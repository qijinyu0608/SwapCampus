# SwapCampus 部署说明

## 1. 文档目的
本说明用于描述当前仓库在本地环境中的真实部署方式，目标是让拿到项目源码的人能够直接完成启动、验证和重置。

当前文档只覆盖已经在仓库中落地的本地部署方案，不包含服务器上线、域名、HTTPS 或镜像仓库等生产化内容。

## 2. 当前部署形态
- 前端：Docker 构建后由 `nginx` 托管静态资源
- 后端：NestJS 单体服务
- 数据库：MySQL 8
- 对象存储预留：MinIO
- 编排方式：`docker-compose.yml`

Compose 当前会启动以下服务：
- `mysql`
- `minio`
- `db-init`
- `backend`
- `frontend`

## 3. 环境要求
- Docker Desktop

补充说明：
- 如果只按当前方式运行，不需要提前安装本机 MySQL
- Node.js 不是 Docker 启动的前置条件，但如果需要单独执行前端构建或后端测试，建议使用 Node 20

## 4. 部署步骤
在项目根目录执行：

```bash
docker compose up -d --build
```

首次启动时会自动完成：
- MySQL 容器启动并创建数据库
- `db-init` 执行 `prisma db push`
- 导入默认种子数据
- 补齐默认管理员账号、演示账号和批量普通用户
- 启动后端服务和前端静态站点

## 5. 访问地址
- 前端首页：`http://localhost:5178`
- 后端健康检查：`http://localhost:3001/api/health`
- Socket.IO 地址：`http://localhost:3001`
- MinIO Console：`http://localhost:9001`
- MySQL：`localhost:3306`

## 6. 默认账号
- 管理员：`admin@swapcampus.cn / SwapCampusAdmin2026`
- 普通演示用户：`qjinyu0608@qq.com / 123456`
- 种子普通用户：`user1@stu.swapcampus.cn` 至 `user30@stu.swapcampus.cn`
- 普通种子用户默认密码：`SwapCampusUser2026`

## 7. 初始化数据口径
当前默认初始化包含：
- 管理员账号：1 个
- 固定普通演示账号：1 个
- 批量种子普通用户：30 个
- 商品：360 条
- 校园服务任务：8 条

说明：
- 如果数据库为空，会执行完整 seed
- 如果数据库不是空库，`db-init` 会补齐默认账号和基础数据，但不会清空现有业务数据

## 8. 启动后验证
### 8.1 容器状态
```bash
docker compose ps
```

预期：
- `mysql`、`backend`、`frontend` 为 `running`
- `db-init` 为 `exited (0)`

### 8.2 后端健康检查
```bash
curl http://localhost:3001/api/health
```

预期返回包含：
```json
{"status":"ok"}
```

### 8.3 查看后端日志
```bash
docker compose logs backend --tail=50
```

预期：
- 不应持续出现数据库连接失败报错
- 不应持续出现 Prisma 初始化失败报错

### 8.4 页面访问验证
手动访问以下页面确认前端已正常启动：
- 首页
- 登录页
- 发布页
- 消息页
- 个人中心
- 后台页

## 9. 常用命令
```bash
make install
make up
make down
make logs
make backend-test
make frontend-build
```

含义：
- `make install`：安装前后端依赖
- `make up`：执行 `docker compose up -d --build`
- `make down`：执行 `docker compose down -v`
- `make logs`：持续查看容器日志
- `make backend-test`：运行后端 Jest 测试
- `make frontend-build`：运行前端构建

## 10. 重置部署
如果需要恢复到初始演示数据，执行：

```bash
docker compose down -v
docker compose up -d --build
```

说明：
- `down -v` 会删除 MySQL 数据卷
- 重新启动后会重新建表并重新导入种子数据

## 11. 当前已知边界
- 聊天能力目前是 HTTP 落库 + Socket.IO 演示级实时推送，不是完整 IM 系统
- 收藏能力当前仍以前端本地存储为主，数据库 `Favorite` 表尚未完整启用
- JWT 鉴权仍是演示口径，未形成完整权限链路
- MinIO 已纳入部署编排，但图片上传闭环尚未完全落地

## 12. 交付建议
- 对外分发时建议直接打包整个 `SwapCampus` 目录
- 至少附带以下文件：
  - `README.md`
  - `docs/部署说明.md`
  - `docs/08-部署与运维手册.md`
- 如果对方只用于演示，优先推荐 Docker 启动方式，不建议先做本机源码分环境部署
