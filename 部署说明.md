# SwapCampus 部署说明

## 1. 文档目的
本说明用于描述当前仓库在本地环境中的真实部署方式，目标是让拿到项目源码的人能够直接完成启动、验证和重置。

当前文档只覆盖已经在仓库中落地的本地部署方案，不包含服务器上线、域名、HTTPS 或镜像仓库等生产化内容。

## 2. 当前部署形态
- 前端：Docker 构建后由 `nginx` 托管静态资源
- 后端：NestJS 单体服务
- 数据库：MySQL 8
- 对象存储预留：MinIO
- 编排方式：`docker-compose.yml`

Compose 当前会启动以下服务：
- `mysql`
- `minio`
- `meilisearch`
- `supertokens-db`
- `supertokens`
- `backend`
- `frontend`

可选 profile：
- `init`：一次性初始化任务 `db-init`

## 3. 环境要求
- Docker Desktop

补充说明：
- 如果只按当前方式运行，不需要提前安装本机 MySQL
- Node.js 不是 Docker 启动的前置条件，但如果需要单独执行前端构建或后端测试，建议使用 Node 20

## 4. 部署步骤
在项目根目录执行：

```bash
make init
make start
```

首次启动时会自动完成：
- MySQL 容器启动并创建数据库
- SuperTokens PostgreSQL 容器启动并创建认证数据库
- `db-init` 执行 `prisma db push`
- 不导入任何默认商品、服务或用户假数据
- 启动 `meilisearch` 与 `supertokens`
- 启动后端服务和前端静态站点

日常开发重启时，直接执行：

```bash
make start
```

## 5. 访问地址
- 前端首页：`http://10.66.0.11:5178`
- 后端健康检查：`http://10.66.0.11:3001/api/health`
- Socket.IO 地址：`http://10.66.0.11:3001`
- MinIO Console：`http://localhost:9001`
- MySQL：`localhost:3306`

## 6. 初始化数据口径
当前默认初始化不再自动写入商品、校园服务和用户演示数据。

说明：
- 如果数据库为空，`db-init` 只执行建表和基础初始化
- 如果数据库不是空库，`db-init` 不会补齐默认账号或演示数据
- 认证账号数据由 `supertokens-db` 持久化，重启 `supertokens` 不会丢失账号

## 7. 启动后验证
### 7.1 容器状态
```bash
docker compose ps
```

预期：
- 默认运行时 `mysql`、`minio`、`meilisearch`、`supertokens-db`、`supertokens`、`backend`、`frontend` 为 `running`
- 若执行过初始化，`db-init` 应为 `exited (0)`

认证异常时可执行：

```bash
make restart-auth
```

这会先清理历史遗留容器 `swapcampus-supertokens-local`，再确保 `supertokens` 已启动，最后重启 `backend`，避免出现“认证服务未就绪，请稍后重试”或 `3567` 端口冲突。

### 8.2 后端健康检查
```bash
curl http://10.66.0.11:3001/api/health
```

预期返回包含：
```json
{"status":"ok"}
```

### 8.3 查看后端日志
```bash
docker compose logs backend --tail=50
```

预期：
- 不应持续出现数据库连接失败报错
- 不应持续出现 Prisma 初始化失败报错

### 8.4 页面访问验证
手动访问以下页面确认前端已正常启动：
- 首页
- 登录页
- 发布页
- 消息页
- 个人中心
- 后台页

## 9. 常用命令
```bash
make install
make up
make down
make logs
make backend-test
make frontend-build
```

含义：
- `make install`：安装前后端依赖
- `make up`：执行 `docker compose up -d --build`
- `make down`：执行 `docker compose down -v`
- `make logs`：持续查看容器日志
- `make backend-test`：运行后端 Jest 测试
- `make frontend-build`：运行前端构建

## 10. 重置部署
如果需要恢复到空库状态，执行：

```bash
docker compose down -v
docker compose up -d --build
```

说明：
- `down -v` 会删除 MySQL 与 SuperTokens PostgreSQL 数据卷
- 重新启动后会重新建表，但不会自动导入任何商品、服务或用户种子数据

## 11. 当前已知边界
- 聊天能力目前是 HTTP 落库 + Socket.IO 演示级实时推送，不是完整 IM 系统
- 收藏能力当前仍以前端本地存储为主，数据库 `Favorite` 表尚未完整启用
- JWT 鉴权仍是演示口径，未形成完整权限链路
- MinIO 已纳入部署编排，但图片上传闭环尚未完全落地

## 12. 交付建议
- 对外分发时建议直接打包整个 `SwapCampus` 目录
- 至少附带以下文件：
  - `README.md`
  - `docs/部署说明.md`
  - `docs/08-部署与运维手册.md`
- 如果对方只用于演示，优先推荐 Docker 启动方式，不建议先做本机源码分环境部署
