# 种子数据计划

目标：重建一套高质量演示数据，覆盖用户、商品、校园服务、订单、消息、收藏、举报、积分与审计日志。

执行命令：

```bash
cd backend && npm run db:reset-and-seed-demo
```

质量要求：

- 商品至少覆盖主要分类，且包含不同成色、价格带和订单状态
- 校园服务至少覆盖请求、提供两类方向，以及线上、线下、私聊三种地点模式
- 订单、会话、消息、举报、积分资产与审计日志都应有样本数据
- 搜索索引需在重建后重新同步
