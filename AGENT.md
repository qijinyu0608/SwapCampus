# Agent Entry

如果你是首次接触本仓库的 agent，且当前没有额外人工指导，请先阅读本文档，再按索引进入 `agent/` 目录中的对应规范。

## 1. 工作原则

在本仓库中，默认按以下规则执行：

1. 先理解结构约束，再改实现
2. 先看 `agent/`，再看 `docs/`
3. 涉及多页面、多模块、多接口的改动，先确认是否已有规范
4. 若改动会突破现有规范，应先更新规范，再落代码
5. 课程文档用于背景与追溯，工程规范以 `agent/` 为准
6. `docs/archive/` 默认视为历史材料，除非任务明确要求追溯，否则不要把它当成当前基线

## 2. 阅读顺序

没有明确任务指引时，按这个顺序阅读：

1. `agent/INDEX.md`
2. `agent/governance/01-工程文档与约束治理规范.md`
3. `agent/domain/02-业务边界与核心需求基线.md`
4. `agent/architecture/04-实现架构与模块职责基线.md`
5. `agent/data/05-数据库与状态模型基线.md`

然后根据任务类型继续进入对应规范。

## 3. 按任务类型索引

### 3.1 做结构重构 / 大范围代码整理

先读：

- `agent/governance/01-工程文档与约束治理规范.md`
- `agent/architecture/04-实现架构与模块职责基线.md`

### 3.2 做商品、分类、发布、推荐相关改动

先读：

- `agent/domain/02-业务边界与核心需求基线.md`
- `agent/domain/23-商品分类体系规范.md`
- `agent/data/05-数据库与状态模型基线.md`
- `agent/data/25-商品数据结构规范.md`

### 3.3 做 UI、样式、设计系统相关改动

先读：

- `agent/ui/24-前端视觉与样式规范.md`
- `agent/architecture/04-实现架构与模块职责基线.md`

### 3.4 做数据库、接口、状态流相关改动

先读：

- `agent/data/05-数据库与状态模型基线.md`
- `agent/data/25-商品数据结构规范.md`
- `agent/data/26-用户数据结构规范.md`
- `agent/domain/23-商品分类体系规范.md`

## 4. 与 `docs/` 的关系

- `agent/`：当前有效的工程规范
- `docs/`：当前有效课程文档、专题分析、验收证明
- `docs/archive/`：历史过程材料、课程表单、已归档专题

当两者表述不一致时：

1. 先看 `agent/` 是否已经定义当前规范
2. 若 `agent/` 没定义，再回看 `docs/`
3. 若发现 `docs/` 与实现或 `agent/` 冲突，应优先修正 `agent/` 或代码，再决定是否同步修正文档
4. 若只有 `docs/archive/` 与现状冲突，默认按历史材料处理，不反向覆盖当前实现口径

## 5. 执行要求

若改动涉及以下内容之一，默认视为“需要按规范办事”的任务：

- 分类体系
- 商品数据结构
- 状态枚举
- 前后端目录结构
- 共享 UI 和 token
- 页面信息架构
- 跨模块统一常量

不要绕开规范直接在页面或局部服务里重新定义一套。
